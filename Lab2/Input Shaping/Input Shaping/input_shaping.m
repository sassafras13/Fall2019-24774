wn = 10;
zeta = 0.01;
K = exp(-zeta*pi/sqrt(1-zeta^2));
A0 = 1/(1+2*K+K^2);
A1 = 2*K/(1+2*K+K^2);
A2 = K^2/(1+2*K+K^2);
t1 = pi/wn/sqrt(1-zeta^2);
t2 = 2*pi/wn/sqrt(1-zeta^2);

% n=4;
% d = log(1.7/1.4)/n;
% 
% z = d/sqrt(2*pi^2 + d^2);

%%
%wn 1.2
wn = 10;
wn = 1.2*wn;
zeta = 0.01;
K = exp(-zeta*pi/sqrt(1-zeta^2));
A0 = 1/(1+2*K+K^2);
A1 = 2*K/(1+2*K+K^2);
A2 = K^2/(1+2*K+K^2);
t1 = pi/wn/sqrt(1-zeta^2);
t2 = 2*pi/wn/sqrt(1-zeta^2);
%%
%wn 1.5
wn = 10;
wn = 1.5*wn;
zeta = 0.01;
K = exp(-zeta*pi/sqrt(1-zeta^2));
A0 = 1/(1+2*K+K^2);
A1 = 2*K/(1+2*K+K^2);
A2 = K^2/(1+2*K+K^2);
t1 = pi/wn/sqrt(1-zeta^2);
t2 = 2*pi/wn/sqrt(1-zeta^2);
%%
%wn 2
wn = 10;
wn = 2*wn;
zeta = 0.01;
K = exp(-zeta*pi/sqrt(1-zeta^2));
A0 = 1/(1+2*K+K^2);
A1 = 2*K/(1+2*K+K^2);
A2 = K^2/(1+2*K+K^2);
t1 = pi/wn/sqrt(1-zeta^2);
t2 = 2*pi/wn/sqrt(1-zeta^2);

%%
%zeta 1.2
zeta = 0.01;
wn = 10;
zeta = 1.2*zeta;
K = exp(-zeta*pi/sqrt(1-zeta^2));
A0 = 1/(1+2*K+K^2);
A1 = 2*K/(1+2*K+K^2);
A2 = K^2/(1+2*K+K^2);
t1 = pi/wn/sqrt(1-zeta^2);
t2 = 2*pi/wn/sqrt(1-zeta^2);
%%
%zeta 1.5
zeta = 0.01;
wn = 10;
zeta = 1.5*zeta;
K = exp(-zeta*pi/sqrt(1-zeta^2));
A0 = 1/(1+2*K+K^2);
A1 = 2*K/(1+2*K+K^2);
A2 = K^2/(1+2*K+K^2);
t1 = pi/wn/sqrt(1-zeta^2);
t2 = 2*pi/wn/sqrt(1-zeta^2);
%%
%zeta 2
zeta = 0.01;
wn = 10;
zeta = 2*zeta;
K = exp(-zeta*pi/sqrt(1-zeta^2));
A0 = 1/(1+2*K+K^2);
A1 = 2*K/(1+2*K+K^2);
A2 = K^2/(1+2*K+K^2);
t1 = pi/wn/sqrt(1-zeta^2);
t2 = 2*pi/wn/sqrt(1-zeta^2);

